bool subs(string a, string &b){ //checks if a is subsequence of b
    int j = 0;
    frw(i, 0, sz(b)){
        if(a[j] == b[i])
            j++;
        if(j == sz(a))  break;
    }
    return j == sz(a);
}
