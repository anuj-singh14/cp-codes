#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define int long long
#define ld long double
#define vi vector<int>
#define pii pair<int, int>
#define vpii vector<pii>
#define frw(i, a, b) for(int i = (a); i < (b); i++)
#define bck(i, a, b) for(int i = (a); i > (b); i--)
#define T() int testcases; cin >> testcases; frw(TC, 1, testcases + 1)
#define tez ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
#define endl "\n"
#define fi first
#define se second
#define pow asdihagsiduh
#define pub push_back
#define in insert
#define all(a) a.begin(), a.end()
#define rall(a) a.rbegin(), a.rend()
#define sz(a) (int)(a.size())
#define sq(a) ((a) * (a))                

#ifdef LOCAL
#include <C:\Programs\prettyprint.hpp>
template <typename ...Ts>
void trace1(Ts ...T){ ((cerr << "{ " << T << " }\n" ), ...); }
#define trace(...) cerr << "[" #__VA_ARGS__ << "] : " << '\n', trace1(__VA_ARGS__), cerr << '\n';
#else
#define trace(...)
#define trace1(...)
#endif

signed main(){
    tez
    
}
